<style>
    /* Add a black background color to the top navigation */
    .topnav {
        background-color: #333;
        overflow: hidden;
    }

    /* Style the links inside the navigation bar */
    .topnav a {
        float: left;
        color: #f2f2f2;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
        font-size: 17px;
    }

    /* Change the color of links on hover */
    .topnav a:hover {
        background-color: #ddd;
        color: black;
    }

    /* Add a color to the active/current link */
    .topnav a.active {
        background-color: #087ab1;
        color: white;
    }

    .tableView table,th,td,tr{
        border: 1px solid black;
        border-collapse: collapse;
    }

    .tableView tr.selrow{
        border: 3px solid black;
        border-collapse: collapse;
    }
</style>
<div class="topnav">
    <a id="dash" class="inactive" href="/Dashboard">Trove</a>
    <a id="calendar" class="inactive" href="/Weekly-Calendar">Calendar</a>
    <a id="accounting" class="inactive" href="/TroveAccounting">Accounting</a>
    <a style="float:right" class="inactive" href="/accSettings/logout">Logout</a>
    <a id="accountset" class="inactive" style="float:right" href="/accSettings">Settings</a>
</div>
<script>
    let loc = window.location.href;
    let url = new URL(loc);
    let path = url.pathname;
    console.log(path);
    switch(path){
        case '/':
            var dbb = document.getElementById("dash");
            console.log('dash');
            break;
        case '/Dashboard':
            var dbb = document.getElementById("dash");
            console.log('dash');
            break;
        case '/Weekly-Calendar':
            var dbb = document.getElementById("calendar");
            console.log('calendar');
            break;
        case '/accSettings':
            var dbb = document.getElementById("accountset");
            console.log('accountset');
            break;
        case '/TroveAccounting':
            var dbb = document.getElementById("accounting");
            console.log("accounting");
            break;
    }
    dbb.className = "active";
</script>